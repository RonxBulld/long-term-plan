{
  "name": "long-term-plan-mcp",
  "version": "0.1.0",
  "description": "MCP server for managing large todo plans stored in structured Markdown.",
  "type": "module",
  "bin": {
    "long-term-plan-mcp": "dist/cli.js",
    "ltp": "dist/ltp.js"
  },
  "scripts": {
    "clean": "node scripts/clean.js",
    "build": "npm run clean && tsc -p tsconfig.json",
    "prepare": "npm run build",
    "sync-skill": "node scripts/sync-skill-long-term-plan.js",
    "skill:sync": "npm run build && npm run sync-skill",
    "deploy:codex": "npm run skill:sync && node scripts/deploy-skills.js --target codex",
    "deploy:claude": "npm run skill:sync && node scripts/deploy-skills.js --target claude",
    "deploy": "npm run skill:sync && node scripts/deploy-skills.js --target both",
    "start": "node dist/cli.js",
    "test": "npm run build && node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.3"
  }
}
